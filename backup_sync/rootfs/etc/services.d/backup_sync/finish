#!/command/with-contenv bashio
# shellcheck shell=bash

exit_code="$1"

if [[ "$exit_code" -ne 0 ]]; then
    bashio::log.fatal "Addon failed, halting"
    exec /run/s6/basedir/bin/halt
fi

bashio::log.info "Addon stopped normally"
