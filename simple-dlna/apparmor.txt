#include <tunables/global>

profile simple-dlna flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability sys_admin,
  capability dac_read_search,

  file,
  signal,

  mount,
  umount,
  remount,

  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /package/** ix,
  /command/** ix,

  /run/{,**} rwk,
  /var/run/{,**} mrwkl,
  /tmp/** mrwk,

  # вместо /dev/** mrwkl

  /dev/sd[a-z][0-9]* r,
  /dev/nvme*n*[0-9]* r,
  /dev/mmcblk[0-9]*p[0-9]* r,
  /dev/disk/by-uuid/** r,
  /dev/disk/by-label/** r,

  # fuse если нужен
  /dev/fuse rw,

  /data/** rw,
  /config/** rw,
  /media/** rw,
  /share/** rw,
}